package co.jco.weatherdemo

import android.support.annotation.IdRes
import android.support.v4.app.Fragment
import android.support.v7.app.AppCompatActivity

/**
 * Helper function to replace a fragment in a transation
 */
fun AppCompatActivity.replaceFragment(@IdRes frameId: Int, fragment: Fragment) {
    //TODO WS3 replace given fragment into given frameId using fragment manager !
}